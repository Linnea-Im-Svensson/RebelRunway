import React from "react";
import FavoriteBtn from "~/components/utils/FavoriteBtn";
import { api } from "~/utils/api";

const brandsPage = () => {
  // const fav = api.favorite.getFavoriteProducts.useQuery().data;
  // console.log(fav);
  return <div></div>;
};

export default brandsPage;
